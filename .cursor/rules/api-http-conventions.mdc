---
description: API 与 HTTP 封装约定（src/api/*.ts + httpGet/httpPost，禁止新增 loader/$api）
globs: src/api/**,src/**/*.vue,src/stores/**,src/services/**
alwaysApply: false
---

# API / HTTP 约定（hos-authserver-web-v3）

完整说明见 `src/api/README.md`、`src/axios/README.md`。

## 必须遵守

1. **新接口**只写在 `src/api/<domain>.ts`，使用 `httpGet` / `httpPost` / `http`（从 `@/axios` 导入）。
2. **组件 / Store** 使用 `import { fetchXxx } from '@/api/...'`，**禁止**新增 `this.$api('module.method')` 或 `apiRequest('loaderKey')`（遗留调用可逐步改，不要扩大范围）。
3. **不要修改** `src/axios/`（拦截器、loader、http 实例），业务变更只改 `src/api/*.ts`。
4. 响应按 `ApiResult<T>` 处理：`if (res.code != 200) return`；`websys` 等特例见对应 api 模块注释。
5. 独立 `baseURL`（如 `websys`、`staff`）在 `http({ baseURL, ... })` 中传入，勿在组件里裸写 axios。

## 新增 API 模板

```ts
// src/api/xxx.ts
import { httpGet, httpPost, http } from '@/axios'

export function fetchSomething(params?: Record<string, unknown>) {
  return httpGet<YourType>('/your/path', params)
}

// POST 且参数走 query（对齐旧 loader 行为时）
export function fetchLegacyStyle(params?: Record<string, unknown>) {
  return http({ method: 'post', url: '/path', params })
}
```

在 `src/api/index.ts` 增加 re-export；函数命名优先 `fetch*` / 动词短语。

## 禁止

- 新增 `src/api/*.js` 或 loader 字符串 key
- 在 `.vue` 里直接 `axios.create` / 写完整 URL（除环境变量 base）
- 为单接口新建 `src/services/`（已废弃，用 `src/api/*.ts`）

## 特殊场景

| 场景 | 做法 |
|------|------|
| `emulateJSON: true` | `http({ emulateJSON: true, data: {...} })` |
| 登录等需自定义 header | `httpPost(url, data, headers)` 第三参 |
| 非标准响应（websys `status`/`rtn`） | 在 api 模块注明，调用方按原逻辑解析 |
