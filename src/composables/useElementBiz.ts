/**
 * Element Plus 业务封装 API（与 useHosBiz 等价，推荐新代码使用）
 */
export {
	openHosBizDialog as openElBizDialog,
	closeHosBizDialog as closeElBizDialog,
	updateHosBizTable as updateElBizTable,
	refreshHosBizTable as refreshElBizTable,
	reloadHosBizTable as reloadElBizTable,
	useHosBiz as useElementBiz,
} from '@/composables/useHosBiz';
